# Сервіс «репорт вразливості» для 
навчальних проектів

Вебзастосунок для реєстрації та управління звітами (Report System). Робота виконана в межах лабораторної роботи №1 — Варіант 6.

## Реалізований функціонал
- Створення: введення даних про інцидент з валідацією.
- Редагування: можливість зміни вже створених записів у окремому фреймі.
- Видалення: видалення неактуальних інцидентів з підтвердженням.
- Сортування та фільтрація: сортування за полями; опціональний фільтр за типом або критичністю.
- Збереження: дані автоматично зберігаються у localStorage браузера.
- Відображення: динамічний рендер таблиці з thead/tbody і заголовками th.
- Події: делегування подій для таблиці для ефективної обробки динамічних рядків.


---

##  Валідація введених даних
- Ім'я користувача — обов'язкове поле до 10 символів;
- Пріоритет — обовʼязкове поле;
- Статус — не обовʼязкове поле;
- опис інциденту — до 4000 символів.

---

### Захист від XSS (Cross-Site Scripting)
Оскільки система призначена для реєстрації кіберінцидентів, користувачі можуть вводити у поля "Опис" або "Репортер" приклади шкідливого коду (payloads).

Щоб запобігти виконанню цього коду в браузері (XSS-атака), реалізовано механізм **Input Sanitization**.
* Перед рендерингом таблиці всі дані проходять через функцію-санітайзер.
* Спеціальні символи замінюються на безпечні HTML-сутності:
    * `<` замінюється на `&lt;`
    * `>` замінюється на `&gt;`
    * `&` замінюється на `&amp;`

**Результат:** Якщо ввести `<script>alert('Hacked')</script>`, застосунок відобразить це як безпечний текст, а не виконає скрипт.

## Використані технології
- **HTML5:** семантична розмітка;
- **CSS3:** Flexbox, змінні, адаптивність;
- **JavaScript:** DOM API, Event Delegation, LocalStorage, JSON.

---

## Запуск проєкту
1. Завантажте репозиторій.
2. Відкрийте файл `index.html` у браузері.
3. Додаткові залежності відсутні.

---

##  Версія
Фінальна версія лабораторної роботи позначена тегом **0.1.0**.
